@startuml Saga Orchestration - User Journey
title Customer Order Journey - Saga Orchestration

start
:Customer submits order;
:System validates order request;
fork
  :Payment Step;
  if (Amount <= $10,000?) then (yes)
    :Generate payment ID;
    :Mark payment completed;
  else (no)
    :Payment rejected;
    stop
  endif
fork again
  note right: Steps execute sequentially\nnot in parallel
end fork
:Inventory Step;
if (Stock available?) then (yes)
  :Reserve inventory;
  :Update stock levels;
else (no)
  :Insufficient stock;
  :COMPENSATE: Refund payment;
  stop
endif
:Shipping Step;
:Generate tracking number;
:Schedule shipment;
:Order COMPLETED;
:Customer receives confirmation;
stop
@enduml
